services:
  - type: web
    name: agentsight-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn product.api_server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: AGENTSIGHT_API_KEYS
        generateValue: true
