venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Model weights — too large for Git (hosted on HuggingFace instead)
src/models/best_agentsight.pth
src/models/baseline_model.pth

# Raw data — too large; splits are included
data/raw/

# Intermediate outputs (reproducible from code)
val_risk_per_step.json
val_risk_per_trajectory.json
val_encoding_check.json

# Logs (large, generated by pipeline)
# Keep: full_training_log.txt, val_ablations.json, test_eval_log.txt, test_predictions.json

# IDE / OS
.vscode/
.idea/
*.DS_Store
Thumbs.db

# LaTeX build artefacts
docs/*.aux
docs/*.bbl
docs/*.blg
docs/*.log
docs/*.out
docs/*.toc
docs/*.synctex.gz
